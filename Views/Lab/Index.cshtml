@model IEnumerable<MvcLabManager.Models.LabManager>

@{
    ViewData["Title"] = "Laboratórios";
}
<h1>@ViewData["Title"]</h1>

@if(Model.Count() == 0)
{
    <p>Nenhum laboratório cadastrado</p>
     <p><a class="asd" asp-area="" asp-controller="Lab" asp-action="Cadastro"><strong>Clique Aqui</strong> para cadastrar um novo laboratório</a></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Número</th>
                <th>Nome</th>
                <th>Bloco</th>
            </tr>
        </thead>
        <body>
            @foreach(var computer in Model)
            {
                <tr>
                    <td>@lab.Id</td>
                    <td>@lab.Number</td>
                    <td>@lab.Name</td>
                    <td>@lab.Block</td>
                    <td>
                        <a class = "btn btn-primary btn-sm" href ="/Lab/Show/@lab.Id">Ver</a>
                        <a class = "btn btn-primary btn-sm" href ="/Lab/Atualizar/@lab.Id">Atualizar</a>
                        <a class = "btn btn-primary btn-sm" href ="/Lab/Delete/@lab.Id">Deletar</a>
                    </td>
                </tr>
            }
        </body>
    </table>
                
}